import { render } from "@testing-library/react";
import { Search } from "../../../src/views/search/Search";

describe("Test in search view", () => {
  test("", () => {
    
  });
  /*
    test("keyword should apper in text", async() => {
        const params = { keyword:"batman"}
      const { getByText } = await render(<Search params={params} />);
      const title = getByText("batman");
      expect(title).toBeInTheDocument();
    
    });
    */
})